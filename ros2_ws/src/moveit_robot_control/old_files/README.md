# old_files

This folder holds archived files that are not part of the current UR + OnRobot pick-place flow.

Current archived files:

- `launch/ur_moveit.launch.py`
  Legacy generic UR MoveIt launch kept for reference and older workflows.

The active files for the current workflow stay in the package root under:

- `launch/`
- `moveit_robot_control_node/`
- `config/`
- `meshes/`

